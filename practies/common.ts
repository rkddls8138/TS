const typeRemoveHypen = (param:string) => {  param.replace(/-/g, '') };
const typeMoveComma = (param:string) => {  param.toString().replace(/,/g, '') };

console.log(typeRemoveHypen("2021-05-28"));

IntersectionObserver

ResizeObserver: confirm.arguments(true);

class interChangeModel {

    user:String


}