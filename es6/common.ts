const removeHypen = (param:number) => { param.replace(/-/g, '') };
const removeComma = (param:number) => { param.toString().replace(/,/g, '') };

IntersectionObserver

ResizeObserver: confirm.arguments(true);

class interChangeModel {

    user:String


}